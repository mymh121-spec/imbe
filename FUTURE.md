# Future work

이 문서는 실제 카메라와 인쇄 마커가 있어야 완료할 수 있는 후속 작업만 관리합니다. 카메라 없이 개발 가능한 Three.js 지휘봉, Web Audio 3트랙 믹서, 제스처 매핑, 안정화, 시뮬레이션, UI와 자동 검증은 현재 범위에서 완료했습니다.

카메라 입력, 손 추적, ArUco 검출과 보드 자세 추정의 기본 코드는 이미 포함되어 있습니다. 아래 항목은 해당 코드를 제거하거나 다시 만드는 계획이 아니라, 실제 하드웨어에서 품질을 검증하고 보강하기 위한 작업입니다.

현재 제품 방향에서 마커는 선택적 고정밀 모드입니다. 상대 위치와 비율 기반 지휘에는 MediaPipe 손 추적을 기본으로 사용하며, 인쇄 마커 검증과 정밀 보드 자세는 첫 사용 버전의 필수 완료 조건으로 보지 않습니다. 우선순위는 실제 손 추적 검증과 사용자 제스처 데이터 수집입니다.

## 현재 범위 구분

| 번호 | 항목 | 상태 |
| ---: | --- | --- |
| 1 | 프로젝트 구조, Vite, Three.js, UI | 완료 |
| 2 | 카메라 입력 | 기본 구현 완료, 실제 장치 검증은 Future |
| 3 | 마커 ID·코너 검출 | 기본 구현 완료, 인쇄 마커 검증은 Future |
| 4 | 다중 마커 자세 추정 | 기본 구현 완료, 실측 정확도 검증은 Future |
| 5 | 가상 지휘봉, 궤적, 유실 처리 | 시뮬레이션 완료, 실물 유실 검증은 Future |
| 6 | 3트랙 Web Audio 엔진 | 완료 |
| 7 | 공통 제스처 매핑 | 완료 |
| 8 | 저역통과, 보간, 오디오 램프 | 완료 |
| 9 | 마우스·키보드 시뮬레이션 | 완료 |
| 10 | 상태·설정·재생 UI | 완료 |
| 11 | 정적 검사, 빌드, 자동 테스트 | 완료 |
| 12 | 설치·실행·보정 문서 | 완료, 실측 권장값은 Future에서 보강 |

## 1. 실제 카메라 호환성 검증

- Edge와 Chrome에서 내장 카메라 및 USB 카메라 권한 허용·거부·재허용을 각각 확인합니다.
- 다른 앱이 카메라를 점유한 경우의 오류 문구와 복구 절차를 확인합니다.
- 카메라 ON/OFF를 반복하거나 새로고침해도 스트림과 트랙이 남지 않는지 확인합니다.

완료 조건: 지원 브라우저와 장치별 결과를 기록하고, 실패 상태에서 앱을 재시작하지 않아도 다시 연결할 수 있어야 합니다.

## 2. 인쇄 마커 검출 검증

- ID 0, 1, 2 마커를 실제 크기로 출력해 ID와 네 모서리 검출을 확인합니다.
- 밝기, 역광, 모션 블러, 카메라 거리와 인쇄 품질별 최소 검출 조건을 측정합니다.
- 3개 중 1개가 가려진 상태와 1개만 보이는 상태에서 동작 품질을 비교합니다.

완료 조건: 권장 마커 크기·거리·조명 범위를 README에 수치로 기록하고, 2개 이상 보일 때 안정적으로 보드 자세를 유지해야 합니다.

## 3. 실제 보드 자세 정확도 측정

- 마커 중심 간 거리를 실측해 보드 설정값과 일치시킵니다.
- 좌우·상하·앞뒤 이동과 Pitch/Yaw/Roll 회전을 기준 위치별로 반복 측정합니다.
- 마커 일부 유실 후 다시 나타날 때 위치와 회전이 튀는 정도를 측정합니다.

완료 조건: 상대 X/Y/Z와 회전 오차, 지터, 재검출 복귀 시간을 기록하고 허용 범위를 정해야 합니다.

## 4. 카메라 캘리브레이션 고도화

- 체커보드 기반 초점거리, 주점과 렌즈 왜곡 계수 입력·저장을 추가합니다.
- 왜곡 보정 전후의 화면 가장자리 포즈 오차를 비교합니다.
- 필요하면 로컬 WASM 기반 `solvePnP` 또는 동등한 포즈 솔버를 검토합니다.

완료 조건: 캘리브레이션 파일을 브라우저에서 불러오고 저장할 수 있으며, 보정 결과가 현재 상대 포즈보다 실제 측정에서 개선되어야 합니다.

## 5. 실물 마커 유실 안전성 검증

- 빠른 손동작과 마커 가림에서 450ms 유지 후 안전 음량 감쇠가 실제로 작동하는지 확인합니다.
- 재검출 순간 패닝, EQ와 마스터 음량이 갑자기 튀지 않는지 측정합니다.
- 급정지 감쇠와 일시정지 옵션을 실제 음악 파일 3개로 확인합니다.

완료 조건: 장시간 시험에서 AudioParam이 범위를 벗어나지 않고, 유실·복귀 시 청감상 클릭이나 급격한 음량 상승이 없어야 합니다.

## 6. 장치 성능 기준 수립

- 720p와 1080p 입력에서 검출 FPS, 프레임 시간과 CPU 사용률을 측정합니다.
- 저사양 노트북에서 손 추적과 마커 추적의 권장 해상도 및 검출 주기를 정합니다.
- 장시간 실행 시 메모리, 카메라 스트림과 AudioNode 누수를 확인합니다.

완료 조건: 최소 지원 사양과 권장 설정을 README에 기록하고 30분 연속 실행 시험을 통과해야 합니다.

## 7. 사용자 제스처 학습 데이터와 개인화 모델

- 현재 규칙 기반 주먹·손바닥·엄지·핀치 명령의 오인식 사례를 랜드마크 데이터로 수집합니다.
- 사용자 동의 후 원본 영상이 아닌 정규화된 21개 손 관절과 명령 라벨만 로컬에 저장합니다.
- 정적 명령은 작은 MLP 또는 1D CNN, 시간 동작은 TCN/GRU 후보를 브라우저에서 비교합니다.
- 모델이 없거나 신뢰도가 낮으면 현재 규칙 기반 판정으로 안전하게 폴백합니다.

완료 조건: 별도 테스트 세션에서 명령 precision 95% 이상, false trigger 분당 1회 미만, 추론 지연 150ms 이하를 만족해야 합니다. 구체적인 데이터와 모델 구조는 `chuhu.md`를 기준으로 합니다.
