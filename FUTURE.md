# Future work

이 문서는 실제 카메라와 사용자 데이터가 있어야 완료할 수 있는 후속 작업만 관리합니다. 앱은 마커 없이 MediaPipe 손 랜드마크를 사용하며, 정밀한 절대 깊이는 제품 목표에서 제외합니다.

## 현재 범위 구분

| 번호 | 항목 | 상태 |
| ---: | --- | --- |
| 1 | 프로젝트 구조, Vite, Three.js, UI | 완료 |
| 2 | 카메라 입력과 오류 처리 | 기본 구현 완료, 실제 장치 검증은 Future |
| 3 | MediaPipe 21개 손 관절 검출 | 구현 완료, 다양한 환경 검증은 Future |
| 4 | 상대 X/Y/Z와 방향 계산 | 완료 |
| 5 | 가상 지휘봉, 궤적, 유실 처리 | 완료 |
| 6 | 3트랙 Web Audio 엔진 | 완료 |
| 7 | 공통 제스처 매핑 | 완료 |
| 8 | 저역통과, 보간, 오디오 램프 | 완료 |
| 9 | 마우스·키보드 시뮬레이션 | 완료 |
| 10 | 상태·설정·재생 UI | 완료 |
| 11 | 정적 검사, 빌드, 자동 테스트 | 완료 |
| 12 | 설치·실행 문서 | 완료 |

## 1. 실제 카메라 호환성 검증

- Edge와 Chrome에서 내장 카메라 및 USB 카메라 권한 허용·거부·재허용을 확인합니다.
- 다른 앱이 카메라를 점유한 경우의 오류 문구와 복구 절차를 확인합니다.
- 카메라 ON/OFF를 반복하거나 새로고침해도 스트림과 트랙이 남지 않는지 확인합니다.

완료 조건: 지원 브라우저와 장치별 결과를 기록하고, 실패 상태에서 앱을 재시작하지 않아도 다시 연결할 수 있어야 합니다.

## 2. 맨손 추적 품질 검증

- 밝기, 역광, 복잡한 배경, 모션 블러와 카메라 거리별 검출률을 측정합니다.
- 왼손·오른손, 피부색, 손 크기와 소매 형태가 달라도 지휘봉 중심과 방향이 안정적인지 확인합니다.
- 손가락 일부 가림과 손 이탈 후 재검출 시 위치가 튀는 정도를 측정합니다.

완료 조건: 권장 조명·거리 범위를 README에 기록하고, 일반적인 지휘 동작에서 손 추적이 지속되어야 합니다.

## 3. 상대 깊이 튜닝

- 손바닥 폭으로 계산한 0~1 상대 Z가 가까이·멀리 동작을 충분히 구분하는지 확인합니다.
- 손 크기가 다른 사용자에게 현재 7~29% 정규화 범위가 적절한지 측정합니다.
- 필요할 때만 사용자의 평상시 손 크기를 한 번 저장하는 간단한 기준점 보정을 추가합니다.

절대 cm 거리, 카메라 내부 파라미터와 렌즈 왜곡 보정은 구현 대상이 아닙니다. 완료 조건은 실제 지휘 중 가까이·멀리 변화가 음량에 일관되게 반영되고 정지 자세에서 지터가 들리지 않는 것입니다.

## 4. 손 유실 안전성 검증

- 빠른 손동작과 가림에서 450ms 유지 후 안전 음량 감쇠가 실제로 작동하는지 확인합니다.
- 재검출 순간 패닝, EQ와 마스터 음량이 갑자기 튀지 않는지 측정합니다.
- 급정지 감쇠와 일시정지 옵션을 실제 음악 파일 3개로 확인합니다.

완료 조건: 장시간 시험에서 AudioParam이 범위를 벗어나지 않고, 유실·복귀 시 청감상 클릭이나 급격한 음량 상승이 없어야 합니다.

## 5. 장치 성능 기준 수립

- 720p와 1080p 입력에서 검출 FPS, 프레임 시간과 CPU 사용률을 측정합니다.
- 저사양 노트북에서 권장 해상도 및 검출 주기를 정합니다.
- 장시간 실행 시 메모리, 카메라 스트림과 AudioNode 누수를 확인합니다.

완료 조건: 최소 지원 사양과 권장 설정을 README에 기록하고 30분 연속 실행 시험을 통과해야 합니다.

## 6. 선택적 개인화 모델

딥러닝은 현재 구현 범위가 아닙니다. 규칙 기반 명령의 오인식이 실제 사용자 시험에서 문제로 확인될 때만 다음 단계를 검토합니다.

- 사용자 동의 후 원본 영상이 아닌 정규화된 21개 손 관절과 명령 라벨만 로컬에 저장합니다.
- 정적 명령은 작은 MLP 또는 1D CNN, 시간 동작은 TCN/GRU 후보를 브라우저에서 비교합니다.
- 모델이 없거나 신뢰도가 낮으면 현재 규칙 기반 판정으로 폴백합니다.
- 별도 테스트 세션에서 명령 precision 95% 이상, false trigger 분당 1회 미만, 추론 지연 150ms 이하를 목표로 합니다.

구체적인 데이터와 모델 구조는 `chuhu.md`를 기준으로 하되, 사용자 데이터가 쌓이기 전에는 모델 의존성을 추가하지 않습니다.
