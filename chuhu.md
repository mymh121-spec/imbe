# 제스처 명령 설계와 딥러닝 적용 계획

이 문서는 손 추적값을 실제 지휘 동작과 오디오 명령으로 연결하기 위한 설계 문서입니다. 현재 앱은 손의 위치·방향·속도·깊이를 규칙 기반으로 매핑합니다. 다음 단계에서는 박자 이벤트와 일회성 명령을 연속적인 오디오 조절과 분리합니다.

## 현재 구현 상태

규칙 기반 1차 구현이 완료되었습니다. `gestureFeatures`가 주먹·손바닥·엄지·핀치를 판정하고, `gestureCommands`가 유지 시간과 쿨다운을 적용하며, `beatTracker`가 지휘 궤적에서 2·3·4박 카운트와 BPM을 계산합니다. 손 명령과 박자 동기화는 각각 안전 스위치를 켰을 때만 오디오에 반영됩니다. 현재는 딥러닝 모델을 사용하지 않으며, 실제 오인식 데이터가 쌓인 뒤 도입 여부를 결정합니다.

## 1. 지휘 동작과 기능 매핑

### 연속 조작

연속 조작은 매 프레임 값을 갱신합니다. 한 번 누른 것으로 처리하지 않으므로 손이 움직이는 동안 자연스럽게 변화합니다.

| 제스처 | 입력 특징 | 오디오 기능 |
| --- | --- | --- |
| 좌우 이동 | 손바닥 중심 X | 스테레오 패닝, 좌우 소리 가중치 |
| 상하 이동 | 손바닥 중심 Y | 저음·중음·고음 EQ 균형 |
| 가까이·멀리 | 손바닥 폭 또는 상대 Z | 전체 음량 |
| 손목 기울기 | 손목-중지 방향, roll/pitch | EQ 변화 강도 |
| 이동 속도 | 최근 위치 차이 / 시간 | 반응 강도, 악센트 |
| 손을 위로 크게 벌림 | 손 이동 폭과 손가락 펴짐 정도 | 크레센도 |
| 손을 아래로 작게 모음 | 손 이동 폭과 손가락 굽힘 정도 | 디크레센도 |

### 박자 이벤트

박자는 손의 한 프레임 모양이 아니라 시간에 따른 궤적으로 판단합니다.

1. 최근 1~2초의 손 중심 위치와 속도를 버퍼에 저장합니다.
2. 수직 속도가 양수에서 음수로 바뀌는 지점 또는 하강 궤적의 최저점을 찾습니다.
3. 연속된 박자 간격을 중앙값으로 평균해 BPM을 계산합니다.
4. 큰 하강 폭, 긴 간격 또는 사용자가 지정한 첫 박자를 다운비트로 표시합니다.
5. 최근 4~8개 간격을 사용해 BPM을 부드럽게 갱신하고, 한 번의 튐은 박자로 세지 않습니다.

초기 버전은 자유 지휘와 2·3·4박 패턴을 지원하고, 박자 패턴은 설정에서 선택합니다. 박자 이벤트는 `beat: { index, bpm, phase, confidence }` 형태로 오디오와 시각화에 전달합니다.

### 일회성 명령

명령은 일정 시간 유지와 재실행 방지 조건을 모두 만족해야 합니다.

| 명령 | 권장 제스처 | 판정 조건 | 실행 기능 |
| --- | --- | --- | --- |
| MUTE | 주먹 | 350~500ms 유지 | 전체 음소거 |
| 음소거 해제 | 손바닥 펴기 | 350~500ms 유지 | 음소거 해제 |
| 템포 증가 | 엄지 위 | 400ms 유지, 쿨다운 800ms | BPM +10 |
| 템포 감소 | 엄지 아래 | 400ms 유지, 쿨다운 800ms | BPM -10 |
| 크레센도 | 핀치 상태로 위로 이동 | 120ms 이상의 상승 궤적 | 강약 +10% |
| 디크레센도 | 핀치 상태로 아래로 이동 | 120ms 이상의 하강 궤적 | 강약 -10% |
| 밸런스 왼쪽 | 핀치 상태로 왼쪽 이동 | 의도적인 수평 궤적 | 좌측 가중치 증가 |
| 밸런스 오른쪽 | 핀치 상태로 오른쪽 이동 | 의도적인 수평 궤적 | 우측 가중치 증가 |

주먹·손바닥·엄지 방향은 MediaPipe 손가락 랜드마크로 판정합니다. 별도의 부착형 마커는 사용하지 않습니다.

## 2. 박자와 명령을 구분하는 상태 머신

딥러닝 결과를 곧바로 `mute()`나 `setTempoBpm()`에 연결하지 않습니다. 다음 중간 계층을 둡니다.

```text
MediaPipe landmarks
        ↓
pose / velocity / finger features
        ↓
beatTracker + gestureCommands
        ↓
conductorState (continuous | beat | command)
        ↓
gestureMapping + AudioEngine
```

`conductorState`의 기본 안전 규칙은 다음과 같습니다.

- `continuous`: 패닝, EQ, 거리 음량처럼 매 프레임 반영
- `beat`: 하강 이벤트가 한 번 발생할 때만 박자 카운트 증가
- `command`: 신뢰도 임계값, 유지 시간, 쿨다운을 모두 통과할 때만 실행
- `unknown`: 손이 가려졌거나 분류 신뢰도가 낮으면 오디오 명령을 실행하지 않음
- 카메라가 사라지면 기존 450ms hold와 안전 감쇠를 계속 적용

이 구조를 사용하면 박자를 세는 하강 동작이 매 프레임 음소거로 오인되는 것을 막을 수 있습니다. 명령 모드 `ARMED/OFF` 토글도 제공해 공연 중 의도하지 않은 명령을 차단하는 것이 좋습니다.

## 3. 딥러닝이 가능한가

가능합니다. 다만 원본 카메라 영상을 큰 이미지 분류 모델에 넣기보다, 이미 MediaPipe가 추출한 21개 손 랜드마크를 입력으로 사용하는 것이 이 앱에 적합합니다.

### 입력 데이터

- 21개 관절의 `x, y, z`: 63개 값
- 손목 기준 상대 좌표와 손바닥 크기 정규화
- 최근 위치 속도와 방향 변화
- 손가락별 펴짐 비율, 주먹/손바닥/엄지 특징
- 최근 30~60프레임의 시간 창(약 0.5~2초)
- 원본 영상은 저장하지 않고 랜드마크와 라벨만 저장

### 모델 선택

| 목적 | 모델 | 이유 |
| --- | --- | --- |
| 주먹·손바닥·엄지 | 작은 MLP 또는 1D CNN | 단일 자세 분류에 충분하고 빠름 |
| 크레센도·디크레센도 | 시간축 1D CNN 또는 GRU | 방향과 속도를 함께 봐야 함 |
| 박자 검출 | 규칙 기반 피크 검출 + 선택적 TCN | 박자 간격은 규칙 기반이 설명 가능하고 안정적 |
| 모든 결과 | 멀티헤드 모델 | `command`, `beat`, `confidence`를 동시에 출력 가능 |

박자까지 전부 딥러닝으로 처리할 수는 있지만, 데이터가 적을 때는 규칙 기반 피크 검출이 더 예측 가능할 수 있습니다. 권장 방식은 손가락 명령은 분류 모델로, 박자와 BPM은 시간 신호 분석으로 시작하는 하이브리드입니다.

## 4. 학습 방법

### 브라우저에서 끝내는 방법

원본 앱의 “Python·NumPy 없이 브라우저 실행” 조건을 지키려면 TensorFlow.js를 로컬 번들로 추가하고, 별도 학습 화면에서 다음 순서로 처리할 수 있습니다.

1. `MediaPipe` 랜드마크를 30~60프레임씩 수집합니다.
2. 사용자가 `beat`, `fist`, `open_palm`, `thumb_up`, `thumb_down`, `crescendo`, `decrescendo`, `unknown` 라벨을 누릅니다.
3. 좌표를 손목 기준으로 이동시키고 손바닥 크기로 나눠 사용자별 크기 차이를 제거합니다.
4. 좌우 반전, 작은 회전, 속도 변화, 랜드마크 노이즈를 데이터 증강합니다.
5. 브라우저에서 작은 MLP/TCN 모델을 학습하고 검증 정확도와 혼동행렬을 표시합니다.
6. 모델을 IndexedDB와 다운로드 가능한 로컬 파일로 저장합니다.
7. 앱 시작 시 모델을 로컬에서 불러와 추론하고, 모델이 없으면 현재 규칙 기반 로직으로 폴백합니다.

브라우저 학습은 가능하지만 노트북 성능과 데이터 양에 따라 시간이 걸립니다. 운영 시에는 학습을 매번 하지 않고, 한 번 만든 모델을 추론에만 사용하는 것이 좋습니다.

### 오프라인 학습 후 브라우저 추론

더 높은 정확도가 필요하면 별도 로컬 학습 환경에서 모델을 학습하고 `ONNX` 또는 TensorFlow.js 형식으로 내보낸 뒤, 앱에서는 `ONNX Runtime Web` 또는 TensorFlow.js로 추론합니다. 이 경우에도 운영 브라우저에는 Python이나 NumPy가 필요하지 않습니다.

프로젝트의 실행 제약을 엄격히 지키려면 Python/NumPy를 앱에 포함하지 말고, 학습 산출물만 `public/models/`에 배포해야 합니다. 학습 환경까지 완전히 브라우저로 제한하려면 앞의 TensorFlow.js 방식으로 통일합니다.

## 5. 데이터 수집 규칙

최소한 다음 정도의 사용자별 데이터를 권장합니다.

- 정적 명령 제스처: 라벨당 100~300개 샘플
- 크레센도·디크레센도: 라벨당 100개 이상의 시간 창
- 박자: 느림·보통·빠름을 포함한 3~5분 연주
- `unknown`과 가림·흔들림·손 이탈 샘플도 충분히 수집
- 학습·검증·테스트를 사람 또는 세션 단위로 분리

같은 세션의 인접 프레임을 학습과 테스트에 섞으면 정확도가 부풀려집니다. 테스트 세션은 반드시 따로 촬영해야 합니다.

## 6. 적용 파일 제안

현재 구조에 다음 모듈을 추가하는 방식이 자연스럽습니다.

- `modules/beatTracker.ts`: 속도 피크, 다운비트, BPM과 박자 phase
- `modules/gestureFeatures.ts`: 랜드마크 정규화와 손가락 특징
- `modules/gestureCommands.ts`: 유지 시간·쿨다운·unknown 처리
- `modules/gestureModel.ts`: 로컬 TensorFlow.js/ONNX 추론과 폴백
- `modules/conductorState.ts`: 연속값·박자·명령 이벤트 통합
- `tests/beatTracker.test.ts`: 합성 궤적의 BPM·박자 카운트 검증
- `tests/gestureCommands.test.ts`: 주먹 유지, 쿨다운, 저신뢰도 검증

## 7. 성공 기준

실제 사용 전에 정확도 하나만 보지 말고 다음을 측정해야 합니다.

- 명령별 precision/recall과 혼동행렬
- 박자 BPM 평균 오차와 박자 누락률
- 명령 오작동 횟수(분당 false trigger)
- 인식 지연 시간
- 손 이탈 후 안전 감쇠 여부

초기 목표는 명령 precision 95% 이상, 공연 중 false trigger 분당 1회 미만, 명령 지연 150ms 이하로 두고 실제 사용자 시험으로 조정합니다. 목표를 만족하지 못하면 모델을 키우기보다 유지 시간, 쿨다운, `unknown` 임계값과 사용자별 캘리브레이션을 먼저 조정합니다.

## 결론

손 동작만으로 박자, 크레센도·디크레센도, 음소거, 템포, 좌우 가중치를 모두 실행하는 것은 기술적으로 가능합니다. 현재 앱은 MediaPipe 랜드마크와 규칙 기반 박자·명령 판정만 사용합니다. 실제 데이터에서 규칙 기반 정확도가 부족할 때만 작은 분류 모델을 추가하고, 결과는 동일한 상태 머신과 신뢰도·쿨다운을 거쳐 `AudioEngine`에 전달하는 방식이 적절합니다.
